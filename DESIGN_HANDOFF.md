# 花生片 Peanup 网站设计交接

> 本文是当前众筹预览站的设计与工程基线。后续 AI、设计师或开发者应先阅读本文，再修改页面、文案、Canvas、图片或部署配置。

## 1. 产品定位与唯一口径

花生片 Peanup 是一款正在打磨中的长方形六色电子纸产品。网站服务于 Kickstarter 发布前的产品预览，不是商城、后台或动画技术 Demo。

网站要依次让访客理解：

1. Peanup 像一张安静、可更新的即时相纸。
2. 照片、文字和日常信息可以在手机本地整理，再通过蓝牙写入。
3. 六色电子纸依靠环境光呈现，断开连接后继续保持画面；户外观感需等待 Peanup 整机验证。
4. Apple 生态是主要体验，但微信小程序和第三方接入为更多设备保留路径。
5. 当前是众筹预览，目标参数与最终量产验证必须明确区分。

### 1.1 规格口径

| 场景 | 必须使用的表达 |
| --- | --- |
| 屏幕 | `3.68 英寸 E6 六色墨水屏` |
| 像素密度 | `约 258.7 PPI`（由 528 × 792 和有效区计算） |
| 厚度 | `3 mm 众筹目标` |
| 重量 | `28 g 众筹目标`，禁止再出现 `289` |
| 续航 | `1 个月以上众筹目标，取决于刷新频率` |
| 磁吸 | `内置磁环，兼容 MagSafe 磁吸配件`；不暗示 Apple 认证 |
| NFC | CUID 仅在系统支持、获得授权并符合当地规则时可用于门禁 |

屏幕口径已经由产品方确认。页面、Docs、SEO 和结构化数据不得再出现 `3.58`、`3.7`、`4.0` 或省略 `E6` 的主规格表达；`E6` 只作为型号标记，不擅自扩写为未经确认的面板品牌或技术全称。

整机无外露接口、BLE 近场传图、Qi 无线充电、iOS App、Apple 快捷指令、微信小程序传图和第三方扩展属于产品目标能力。不要写成已经量产、已经取得认证或已经完成所有真机验证。

品牌固定使用 `花生片 Peanup`。Peanup 来自 Peanut 的亲切感与 Up 的活力。当前核心表达是“把喜欢的画面，留在眼前”，不要把三幅自然场景写成产品只能显示的内容。

## 2. 视觉系统

### 2.1 设计方向

整体是“纸白编辑式产品画册 + 现代东方排版 + 六色电子纸材料感”。亮色是默认主稿，暗色是同一画册的反相校样；两者都必须让真实产品成为唯一主角，网页动效只负责解释画面如何被写入和保持。

视觉关键词：

- 大面积纸白或近黑留白，避免中灰页面泥成一片
- 墨黑正文、灰色辅助信息和 1px 分隔线
- 宋体大标题建立纸张与东方气质
- 系统无衬线正文保证阅读和加载速度
- 等宽小字用于协议、编号、状态和技术标签
- 非对称编辑排版与真实产品摄影
- 六色只作为电子纸颜料与内容，不做彩虹渐变或装饰性撞色

禁止在页面中途无缘由插入黑色整屏、紫蓝渐变、发光光斑、玻璃卡片堆叠、大圆角 SaaS 卡片或连续大面积灰色工具面板。暗色只能由全站主题开关统一进入，不能成为某个分区的孤立装饰。生活摄影仍是内容节奏中唯一明显的色彩转换。

### 2.2 色彩 Token

基础变量位于 `src/styles/base.css`：

| Token | 值 | 用途 |
| --- | --- | --- |
| `--paper` | `#ffffff` | 页面与主要分区 |
| `--ink` | `#141414` | 标题、正文和产品轮廓 |
| `--muted` | `#6d6d67` | 辅助正文与状态 |
| `--line` | `#d9d9d3` | 分区线、表格和细边框 |
| `--blue` | `#4779bc` | 技术编号、焦点和蓝色颜料 |
| `--red` | `#d64e3e` | 六色电子纸红色 |
| `--yellow` | `#e6b930` | 品牌花生标记与黄色颜料 |
| `--green` | `#6d9666` | 六色电子纸绿色 |
| `--hardware-back` | `#ecece8` | 硬件能力卡背面 |

暗色覆盖由 `html[data-theme="dark"]` 提供，核心值为 `--paper: #0e0f0e`、`--ink: #f1f1ed`、`--muted: #aaa9a2` 和 `--line: #343532`。结构性表面使用 `--surface`、`--surface-soft`、`--surface-raised`、`--surface-hover`、`--chrome-bg` 与 `--menu-bg`，不要在组件里继续散落新的纯白或纯黑值。

电子纸模拟使用低饱和设备色板。纸白不是发光纯白，黑色接近炭黑；但设备有效区在四幕之间必须保持同样明亮，不能在第四幕突然变脏或变灰。

### 2.3 黑白主题

- `src/components/ThemeToggle.astro` 是唯一主题开关，使用带太阳、月亮图形的滑轨与圆形滑块，不引入图标包或文字胶囊。
- 开关位于产品页语言菜单旁，同时出现在 Docs 与 404 页头；使用原生 `button`、本地化 `aria-label` 和 `aria-pressed`。
- 首次访问跟随 `prefers-color-scheme`，手动切换保存到 `localStorage` 的 `peanup.theme`，刷新与跨页面导航后继续沿用。
- `BaseLayout.astro` 在首屏样式应用前写入 `data-theme`，避免由白到黑的闪烁；`chrome.ts` 负责控件状态、主题色和系统偏好变化。
- 暗色只反相页面纸张、文字、线框和结构性面板。产品摄影、E6 六色色板、电子纸有效区、手机模型和硬件实体样板不得套用全局反相滤镜。
- 浅色切换到暗色时，先并行等待暗色首图解码与黑幕从顶部向下覆盖：幕布动画为 360ms，若图片解码更慢则保持全黑等待。主题切换后，黑幕用 400ms 向下退出，暗色首图在幕布后以 500ms 自上而下写入。暗色切换到浅色时，先等待亮色首图解码，再立即以黑幕覆盖当前页；主题切换后，黑幕和亮色首图都用 560ms 向上揭开。两张首图都以高优先级出现在首屏 DOM，切换前仍必须调用 `decode()`；`prefers-reduced-motion` 下直接切换，不播放幕布动画。
- 暗色抠图的透明画布留白略大，首图以顶部中心为基准使用 `translateY(-4.3%) scale(1.04, 1.08)` 做视觉补偿，不能简单按文件像素宽度对齐。404 页在明暗主题下都使用透明抠图，避免亮色摄影底板形成方形白边。

### 2.4 字体与尺寸

- 展示标题：`Songti SC`、`STSong`、`Yu Mincho`、Georgia 或 serif。
- 正文、导航、按钮：Apple 系统字体栈。
- 技术标签：系统等宽字体。
- 所有文字保持 `letter-spacing: 0`。
- 正文通常为 14–16px；备注、Tab、规格标签不能缩到难以阅读。
- Hero 大字只用于真正的首屏，不进入紧凑卡片、侧栏或工具栏。
- “足够轻巧，也足够完整。”规格标题使用 Iowan Old Style / 宋体轻字重，两行独立成块并让第二行轻微缩进；不得退回粗黑无衬线或把两行强行拉宽。

通用圆角上限为 6px。分区依靠留白、细线和网格建立层级，不把每段内容包成卡片。阴影只表达实体高度，不制造霓虹悬浮感。

## 3. 信息架构

首页顺序不得随意打乱：

1. **Hero**：品牌、核心表达、真实产品、`Coming to Kickstarter` 和四项规格带。
2. **会更新的纸**：六色材料、反射式环境光观看与断电保持。
3. **屏内四幕**：三个代表场景与一项在线体验。
4. **生活场景**：真实摄影把产品放回日常。
5. **内容玩法**：照片、日历、天气与书摘。
6. **Apple 与跨平台路径**：iOS、快捷指令、App 内传输进度、微信小程序和第三方扩展。
7. **计算留在手机上**：整理、六色成像、BLE 分块传输、刷屏保持。
8. **抖动系统**：Ordered、误差扩散和感知量化。
9. **硬件能力**：BLE、CUID NFC、Qi 和磁吸摆放。
10. **紧凑规格**：完整众筹目标规格与必要限定。
11. **实体设计与品牌故事**：产品摄影、Peanup 含义与研发状态。
12. **页脚**：Docs、GitHub、`i@caiths.com` 与版权。

公开产品页面优先写结果和体验，不放协议风险清单。中文公开产品说明位于 `/docs/`；中文工程文档位于 `/docs/engineering/`。英文对应页面位于 `/en/docs/` 与 `/en/docs/engineering/`。

## 4. I18N 与路由

产品页使用 `src/i18n/product.ts` 中的类型化构建期字典，核心类型为 `Locale`、`ProductCopy`、`ProductSpec` 和 `CanvasSceneCopy`。不要引入 i18next、React 或运行时翻译库。

| 语言 | 路由 |
| --- | --- |
| 简体中文 | `/` |
| English | `/en/` |
| 繁體中文 | `/zh-tw/` |
| 日本語 | `/ja/` |
| Deutsch | `/de/` |
| Français | `/fr/` |

根路径的静态内容与 `x-default` 为简体中文。首次进入根路径时会先读取用户保存的语言，其次根据可识别的 IANA 时区与 UTC 偏移判断地区，最后参考浏览器语言；无法判断时保持简体中文。手动语言选择写入 `peanup.locale`。语言菜单只使用文字名称，不用国旗。新增文案时必须检查德语长词、法语换行、日语断行和中英文标点。

## 5. 屏内四幕

四幕是 Peanup 电子纸内容，不是网页背景。原生 Canvas 2D 必须严格限制在产品显示区域内，禁止扩展为首屏或全页装饰动画。场景内部以厂家面板的 528 × 792 像素矩阵组织细节；网页舞台继续使用更适合观看的 3:4 编辑式展示比例，不把 3.68 英寸物理尺寸换算成 CSS 尺寸。

### 5.1 统一写屏过渡

`01 ↔ 02 ↔ 03 ↔ 04` 的任意双向切换都使用同一套 880ms 写屏进度和扫描线：

1. `01–03` 离场时缓存旧 Canvas 帧；`04` 离场时保留完整在线工作区 DOM。
2. 准备目标帧。
3. 以自下向上的写屏遮罩和扫描线替换旧帧。
4. 完成后保持静态画面。
5. 进入 04 时，在线工具栏在写屏结束后再淡入。

进入 04 时在线工作区按同一进度由下向上展开；离开 04 时则按该进度向上收起，因此视觉节奏与 Canvas 快照一致，但不会把可交互表单错误地复制进 Canvas。滚动与点击共享同一场景状态；点击 Tab 时先锁定目标幕并立即开始写屏，再平滑移动页面，不能在途中重放经过的其他幕。左侧四个原生按钮必须支持点击、Enter、Space 和触摸，不允许 03 → 04 使用另一套时序，也不允许切换后跳回第一幕。

画面左下使用“功能 / 场景”结构，例如“自由创作 / 飞行”；右下单独显示 `01 / 04` 至 `04 / 04`，不要在左右两处重复页码。舞台画框沿用线上认可的视觉尺寸：桌面约 `510 × 680px`、移动端约 `315 × 420px`。它是用于看清内容和完成交互的演示舞台，不按真实 3.68 英寸尺寸或面板外形比例硬缩。

### 5.2 01 六色云雨

- 初始以六色云彩为主，云层不能遮满屏幕。
- 继续滚动后，48 个大小、长度与节奏不同的六色雨滴分段下落。
- 雨滴是独立细长形状，没有连接线，不形成永久悬挂的帘子。
- 雨量是淅淅沥沥，不是暴雨。
- 横向风偏移只由滚动触发；停止滚动后雨滴静止，不做无限循环。

### 5.3 02 日常信息与花枝

- 5 条花枝从花瓶附近向光生长，花瓶必须保留。
- 枝条在小屏幕里需要清晰，但不能过长、过粗或越过有效区。
- 左上显示日期与天气，下一层承载书摘。
- 场景完成短时生长后保持，不做无意义循环摇摆。

### 5.4 03 飞燕与琴弦

- 30 只黑色飞燕分批沿弧线进入，不能遮黑整块屏幕。
- 每 5 只中有 1 只在飞行后停到谱线附近，即最终保留 6 只停鸟；其余飞出画面。
- 保留六色场景关系，不能退化为单一黄色画面。
- 点击、触摸或按住拖动会在指针附近触发约 900ms 的拨弦余波；动效结束后回到静态。

### 5.5 04 在线体验

- 在线体验直接位于同一块产品屏幕，不另建工作台页面。
- 照片选择区与文字输入区始终并排显示，不要求访客先发现模式开关。
- 支持选择或拖入 JPG、PNG、WebP、AVIF，以及输入短文字。
- 图像只在浏览器本地裁切、六色量化与抖动，不上传。
- 保持原始比例并覆盖式裁切，禁止压扁画面。
- 工具栏、提示和 Tab 的字号、边距与前三幕一致。
- 240 字以内的文本需要在 528 × 792 画布内自动换行与缩放；超出可读安全区时使用省略号提示，不能静默丢失末尾内容。

### 5.6 Canvas 运行边界

- 当前三幕自然场景保留 `heroSketch.ts` 原有的 p5 实例模式绘图语义，但由项目内 `miniP5.ts` 提供所需的最小 Canvas API，不再加载完整 p5 包或 `@types/p5`。动画仍随四幕接近视口后延迟加载；12 种雨滴轮廓合并在本地 `/assets/rain-drops.webp` 图集里，只产生一次素材请求，没有第三方运行时请求。
- `miniP5.ts` 只覆盖四幕实际使用的生命周期、路径、文字、图片染色、高 DPI Canvas 和确定性随机数。扩展动画时应按需补充这一兼容层，禁止直接恢复完整 p5 依赖。
- 逻辑画布固定为 `528 × 792`，设备像素比最多取 `2`；桌面绘制上限为 42 fps，窄屏为 30 fps。
- 纸纹与六色层使用缓存；仅在滚动、切幕、拨弦和短时入场期间请求动画帧。
- Canvas 离开视口或页面切到后台时暂停。
- `prefers-reduced-motion` 下直接完成场景和写屏状态，不播放长动画。
- 素材由代码绘制，不把第三方网络可用性变成页面依赖。

## 6. 电子纸处理与技术展示

电子纸不是普通照片加噪点。当前表现分为：

1. 将输入映射到校准后的六色颜料色板。
2. 使用 Ordered、Floyd–Steinberg、Stucki 或感知量化等方式重建明暗和局部颜色。
3. 在设备外壳内加入克制纸纹、写屏扫描线和实体阴影。

Canvas 使用 528 × 792 内部逻辑画幅并按设备像素比绘制。该数值对应厂家面板像素矩阵，但不能单独用于推导真实 BLE 负载或传输时间。外层 CSS 使用 3:4 产品舞台保证网页构图与可读性，这只是演示比例，不是新的硬件规格；照片仍使用明确的 `cover` 或 `contain`，禁止无意压扁内容。

“计算留在手机上”四步必须保持因果关系：

- `01 整理画面`：动漫头像或壁纸输入。
- `02 六色成像`：同一构图经过算法转换，不能改变高度或过曝。
- `03 分块传输`：已打包像素通过 BLE 发送。
- `04 刷屏并保持`：真实生活照片置于带 PEANUP 标签、内边距和阴影的设备边框。

## 7. 硬件能力卡

“画面一直在，连接只在更新时”下方四项使用统一灰阶硬件组件：

- 正面纯白，1px 灰线和轻阴影。
- 背面固定使用 `#ecece8`，不使用黄、红、绿、蓝顶边或撞色反转。
- 图标为石墨灰 56–64px，只使用四个本地 SVG：Bluetooth、NFC、BatteryCharging、Magnet。
- 悬浮、点击、触摸、Enter 和 Space 均可翻面，`aria-pressed` 同步状态。
- 焦点只使用低饱和 Peanup 蓝。
- `prefers-reduced-motion` 下用正反面淡入切换，不做 3D 旋转。

不要为四个图标重新安装完整图标包。

## 8. 响应式规则

必须检查以下基准视口：

- `1440 × 900`
- `1366 × 1024`
- `1024 × 1366`，同时代表 iPad 竖向布局
- `390 × 844`
- `360 × 800`

移动端不是桌面的等比缩小：

- Hero 使用稳定文档流与网格，标题、产品图、状态和规格带不能互相挤压。
- 四幕先给出标题与四个 Tab，再显示完整设备画板。
- 技术流程、抖动、硬件能力、规格和 Docs 转为单列或受控双列。
- 正文保持可读字号，触摸目标足够大；长词换行，不允许按钮文本溢出。
- 固定格式组件设置 `aspect-ratio`、网格轨道或 min/max 约束，防止动态内容引发布局跳动。
- 页面不得出现水平溢出、文字遮挡、Canvas 空白或图片异常裁切。

## 9. 图片、加载与保护边界

网页资产位于 `public/assets/`。产品、手机和手持图的固有尺寸分别为：

- 产品：`740 × 1120`
- 手机：`1122 × 1402`
- 手持：`1122 × 1402`

资产规则：

- 产品必须在第一屏明确出现。
- AVIF 优先、WebP 回退；首屏高优先级，其余 `loading="lazy"` 与 `decoding="async"`。
- 图片不沿用 Potato Card、TodooCam 或其他旧品牌文字。
- 不使用 PDF 整页截图，不存摄影原片，只保留网页需要的分辨率。
- 所有展示图片补齐真实 `width`、`height`、alt 和合适裁切。
- OG 使用单独的低分辨率品牌分享图，不直接暴露高质量产品摄影。

所有 `img` 使用 `draggable="false"`；`img`、`picture` 和 `.protected-media` 使用 `user-select:none`、`-webkit-user-drag:none` 与 `-webkit-touch-callout:none`。所有页面共享的 `chrome.ts` 再次把图片设为不可拖拽，并只在事件目标属于 `img`、`picture` 或 `.protected-media` 时阻止 `dragstart` 与 `contextmenu`；它不是全站禁右键。页面使用 `noimageindex`、`max-image-preview:none`。`/assets/*` 的 `X-Robots-Tag` 由 `vercel.json` 或生产 Nginx 提供，Astro 静态文件本身不会自动带上该响应头。

这是体验和索引控制，不是 DRM；`noimageindex` 与 `X-Robots-Tag` 也只对遵守规则的爬虫有效。公开网页无法阻止 Network、缓存、源码或截图取得已经传输的图片。禁止使用 Base64、透明遮罩或全站禁右键冒充真正的下载保护；正文选择、链接、键盘导航和屏幕阅读器不能因此受损。

## 10. 性能架构

Astro 负责静态 HTML、SEO 与多语言路由。屏内三幕使用本地 `miniP5.ts` 驱动原生 Canvas 2D，不携带完整 p5 运行时或类型包；不引入运行时翻译框架、图标包或静态 Vercel adapter。

样式分层：

- `src/styles/base.css`：Token、全局壳层、导航、页脚、媒体保护和返回顶部。
- `src/styles/global.css`：产品首页布局、场景和响应式状态。
- `src/styles/docs.css`：产品文档与工程文档。

`src/scripts/lazyFeatures.ts` 负责接近视口时加载：

- `epaperShowcase.ts`：六色色板与三种抖动样例；两个区域任一接近视口都只加载同一个异步模块，确保深链接不会留下空白 Canvas。
- `heroSketch.ts`、`miniP5.ts` 与 `interactiveScreen.ts`：延迟加载的三幕绘图、轻量 Canvas 兼容层与在线预览。
- `epaperPipeline.ts`：图像转换流程。
- `hardwareCubes.ts`：硬件翻转交互。

常规动态模式下，硬件翻转交互在组件接近视口时加载；`prefers-reduced-motion` 开启时则立即加载，让无 3D 的淡入切换在首次键盘、触摸或鼠标操作前就已可用。

性能约束：

- 首屏传输目标仍为 `<70 KB`，但不得把它描述成已经达成。2026-07-28 本地构建中，产品页 HTML、两份 CSS 与常驻 JS 按 gzip 合计约 `27.0 KB`；亮/暗两张首图 AVIF 合计 `63,727 B`，粗略首屏合计约 `90.7 KB`，未计响应头且浏览器 Brotli 结果可能不同。
- 首屏与常驻交互 JavaScript 目标 `<50 KB gzip`；三幕场景必须保持独立异步 chunk，不能进入首屏关键路径。
- 2026-07-28 当前构建基线：三幕 chunk 为 `26,872 B`、约 `9.9 KB gzip`；六个交互 chunk 合计 `40,018 B` 原始、逐文件合计约 `16.7 KB gzip`。雨滴图集为 `4,930 B`，只在四幕接近视口后请求。这组数据包含当前 528 × 792 材料处理、停鸟、可见区暂停与统一 04 过渡，后续应在不减少视觉效果的前提下控制增长。
- 零第三方运行时请求。
- 非首屏图片与交互按需加载，首屏产品图不懒加载。
- 不为简单动效新增框架，不盲删脚本使用的动态状态类。
- `/assets/*` 使用短缓存与 `stale-while-revalidate`；带哈希的 `/_astro/*` 才长期 immutable。

## 11. Docs 与 SEO

- `/docs/` 是默认中文产品说明，使用 `TechArticle`。
- `/docs/engineering/` 是默认中文工程文档。
- `/en/docs/` 与 `/en/docs/engineering/` 保留英文产品与工程文档。
- Product JSON-LD 只出现在六个产品页。
- 每页提供绝对 canonical、正确 locale、hreflang 和 `x-default`。
- 产品页的 `x-default` 为 `/`；产品 Docs 的 `x-default` 为 `/docs/`；工程 Docs 的 `x-default` 为 `/docs/engineering/`。
- `sitemap.xml` 应包含六个产品路由和四条 Docs 路由，`robots.txt` 指向 sitemap。

Docs 与产品页共享纸白/近黑编辑式主题和移动端可访问目录，不把公开产品文档重新变成逆向工程报告。四条 Docs 路由的固定顶栏和移动菜单都必须保留明确的“产品页 / Product”入口，中文返回 `/`，英文返回 `/en/`，不能只依赖品牌标志作为隐式出口。

## 12. 文件责任范围

| 文件 | 职责 |
| --- | --- |
| `src/components/product/ProductPage.astro` | 六语言共享的产品页结构 |
| `src/i18n/product.ts` | 类型化语言元数据与全部产品文案 |
| `src/pages/index.astro` 与语言目录 | 各静态路由入口 |
| `src/pages/docs.astro` | 默认中文公开产品文档 |
| `src/pages/docs/engineering/index.astro` | 默认中文工程文档 |
| `src/pages/en/docs/index.astro` | 英文公开产品文档 |
| `src/pages/en/docs/engineering/index.astro` | 英文工程文档 |
| `src/layouts/BaseLayout.astro` | canonical、hreflang、JSON-LD、OG 与全局壳层 |
| `src/components/ThemeToggle.astro` | 六语言共享的黑白主题开关与无障碍标签 |
| `src/styles/base.css` | 全站基础样式 |
| `src/styles/global.css` | 产品页设计与响应式样式 |
| `src/styles/docs.css` | Docs 专属样式 |
| `src/scripts/heroSketch.ts` | 云雨、花枝、飞燕和统一写屏过渡的视觉与时序参数 |
| `src/scripts/miniP5.ts` | 四幕专用的轻量 p5 兼容 API、Canvas 生命周期和高 DPI 绘制 |
| `src/scripts/interactiveScreen.ts` | 第四幕照片与文字本地预览 |
| `src/scripts/epaperShowcase.ts` | 六色色板和材料样例 |
| `src/scripts/epaperPipeline.ts` | 技术流程六色转换与入场状态 |
| `src/scripts/hardwareCubes.ts` | 四个硬件组件的翻转与键盘交互 |
| `src/scripts/site.ts` | 内容模式、慢速滚动与媒体交互保护 |
| `src/scripts/chrome.ts` | 主题状态、页头状态与返回顶部 |
| `src/scripts/lazyFeatures.ts` | 非首屏功能按需加载 |
| `src/components/HardwareIcon.astro` | 四个本地硬件 SVG 图标 |
| `.github/workflows/deploy.yml` | 构建、不可变 release、原子激活、公网验收与条件回滚 |
| `docs/DEPLOYMENT.md` | 宝塔 / Nginx、TLS、人工验收和回滚手册 |

## 13. 禁止回退的决策

- 不把 Canvas 动画移到产品屏幕外，也不让三幕运行时进入首屏同步加载。
- 不恢复完整 `p5` 或 `@types/p5` 依赖；新增绘图能力时只在 `miniP5.ts` 中补充实际需要的 API，并先做四幕视觉回归。
- 不把三个演示场景描述成产品能力上限。
- 不让雨滴持续循环，不加雨滴连接线，不用巨型云层遮满屏幕。
- 不删除花瓶，不让花枝越过设备边界。
- 不把飞燕画面改成单一黄色或删除黑色主体。
- 不单独新增工作台替代第四幕屏内体验。
- 不让 03 → 04 使用独立过渡；所有场景必须共享写屏逻辑。
- 不把暗色做成孤立黑色分区，不在灰阶硬件卡中突然加入彩色撞色。
- 不把目标规格、平台兼容或 CUID 门禁写成无条件保证。
- 不加入原始摄影文件、外部雨滴请求或完整图标包。
- 不用图片保护措施损害正文选择、键盘或辅助技术。
- 不重置用户工作区，不提交或推送尚未审阅的修改。

## 14. 验收清单

每轮修改后至少运行：

```bash
npm run check
npm run build
git diff --check
```

浏览器验收：

- 六个产品语言路由和四条 Docs 路由均可直接打开。
- Hero 在五个基准视口无重叠、异常裁切或 CLS。
- 四个 Tab 可滚动、点击、触摸和键盘双向切换。
- 01 ↔ 02 ↔ 03 ↔ 04 均使用自下向上的统一刷新。
- 停止滚动后雨滴静止；离开四幕和切后台后 Canvas 不持续绘制。
- 04 可处理照片和文字，源照片不会离开浏览器。
- 04 离场后工具栏应随写屏完全收起；返回时只在写屏完成后出现。
- 六色转换不拉伸、不过曝且保留主要细节。
- 硬件卡可悬浮、点击、触摸和键盘翻转；减少动态时使用淡入。
- 图片不可选中、拖拽或触发受保护区域右键图片菜单，正文交互不受影响。
- 控制台无相关错误，页面无水平溢出。
- canonical、hreflang、x-default、JSON-LD、robots、sitemap 与缓存头正确。
- 三幕运行时保持为四幕附近才加载的独立 chunk；构建结果不包含完整 p5、外部运行时请求或未使用的图标包。
- 主题按钮在产品页、Docs 和 404 均可点击，手动主题刷新后保留；产品摄影与电子纸有效区不被滤镜反相。

## 15. 当前审阅边界

本轮整体打磨完成后先交给用户审阅。未经明确确认，不执行 Git commit 或 push。后续代理必须保留当前未提交内容，并在修改前重新读取工作区状态。

## 16. 客户文案规则

- 一句话只讲一个重点。先说客户能得到什么，再说明技术如何实现。
- 首页不堆叠“链路、路径、能力、接入、验证”等内部工程词汇。算法名称和精确规格可以保留，但解释必须使用日常语言。
- 限制条件独立成句。`3 mm`、`28 g` 和一个月以上续航始终标为 Kickstarter 目标，续航同时注明取决于刷新频率。
- CUID NFC 必须同时说明系统支持、管理方授权和当地规则；MagSafe 只描述配件兼容，不暗示 Apple 认证。
- 英文和其他语言应按当地读者的自然表达重写，不逐字翻译中文。德语等长文本必须在移动端单独检查换行。
- `/docs/` 与 `/en/docs/` 面向客户，保持短句和任务导向；`/docs/engineering/` 保留工程精度，不做营销化改写。

## 17. 一句话设计判断

> 它是否让 Peanup 更像一张安静、真实、会更新的六色相纸，而不是让网页本身抢走产品的注意力？
