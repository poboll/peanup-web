# 花生片 Peanup 网站设计交接

> 本文是当前众筹预览站的设计与工程基线。后续 AI、设计师或开发者应先阅读本文，再修改页面、文案、Canvas、图片或部署配置。

## 1. 产品定位与唯一口径

花生片 Peanup 是一款正在打磨中的长方形六色电子纸产品。网站服务于 Kickstarter 发布前的产品预览，不是商城、后台或动画技术 Demo。

网站要依次让访客理解：

1. Peanup 像一张安静、可更新的即时相纸。
2. 照片、文字和日常信息可以在手机本地整理，再通过蓝牙写入。
3. 六色电子纸依靠环境光呈现，断开连接后继续保持画面；户外观感需等待 Peanup 整机验证。
4. Apple 生态是主要体验，但微信小程序和第三方接入为更多设备保留路径。
5. 当前是众筹预览，目标参数与最终量产验证必须明确区分。

### 1.1 规格口径

| 场景 | 必须使用的表达 |
| --- | --- |
| 屏幕 | `3.68 英寸 E6 六色墨水屏` |
| 像素密度 | `约 258.7 PPI`（由 528 × 792 和有效区计算） |
| 厚度 | `3 mm 众筹目标` |
| 重量 | `28 g 众筹目标`，禁止再出现 `289` |
| 续航 | `1 个月以上众筹目标，取决于刷新频率` |
| 磁吸 | `内置磁环，兼容 MagSafe 磁吸配件`；不暗示 Apple 认证 |
| NFC | CUID 仅在系统支持、获得授权并符合当地规则时可用于门禁 |

屏幕口径已经由产品方确认。页面、Docs、SEO 和结构化数据不得再出现 `3.58`、`3.7`、`4.0` 或省略 `E6` 的主规格表达；`E6` 只作为型号标记，不擅自扩写为未经确认的面板品牌或技术全称。

整机无外露接口、BLE 近场传图、Qi 无线充电、iOS App、Apple 快捷指令、微信小程序传图和第三方扩展属于产品目标能力。不要写成已经量产、已经取得认证或已经完成所有真机验证。

品牌固定使用 `花生片 Peanup`。Peanup 来自 Peanut 的亲切感与 Up 的活力。当前核心表达是“把喜欢的画面，留在眼前”，不要把三幅自然场景写成产品只能显示的内容。

## 2. 视觉系统

### 2.1 设计方向

整体是“纸白编辑式产品画册 + 现代东方排版 + 六色电子纸材料感”。亮色是默认主稿，暗色是同一画册的反相校样；两者都必须让真实产品成为唯一主角，网页动效只负责解释画面如何被写入和保持。

视觉关键词：

- 大面积纸白或近黑留白，避免中灰页面泥成一片
- 墨黑正文、灰色辅助信息和 1px 分隔线
- 宋体大标题建立纸张与东方气质
- 系统无衬线正文保证阅读和加载速度
- 等宽小字用于协议、编号、状态和技术标签
- 非对称编辑排版与真实产品摄影
- 六色只作为电子纸颜料与内容，不做彩虹渐变或装饰性撞色

禁止在页面中途无缘由插入黑色整屏、紫蓝渐变、发光光斑、玻璃卡片堆叠、大圆角 SaaS 卡片或连续大面积灰色工具面板。暗色只能由全站主题开关统一进入，不能成为某个分区的孤立装饰。生活摄影仍是内容节奏中唯一明显的色彩转换。

### 2.2 色彩 Token

基础变量位于 `src/styles/base.css`：

| Token | 值 | 用途 |
| --- | --- | --- |
| `--paper` | `#ffffff` | 页面与主要分区 |
| `--ink` | `#141414` | 标题、正文和产品轮廓 |
| `--muted` | `#6d6d67` | 辅助正文与状态 |
| `--line` | `#d9d9d3` | 分区线、表格和细边框 |
| `--blue` | `#4779bc` | 技术编号、焦点和蓝色颜料 |
| `--red` | `#d64e3e` | 六色电子纸红色 |
| `--yellow` | `#e6b930` | 品牌花生标记与黄色颜料 |
| `--green` | `#6d9666` | 六色电子纸绿色 |
| `--hardware-back` | `#ecece8` | 硬件能力卡背面 |

暗色覆盖由 `html[data-theme="dark"]` 提供，核心值为 `--paper: #10110f`、`--ink: #f2f2ed`、`--muted: #a6a7a0` 和 `--line: #30312d`。结构性表面使用 `--surface`、`--surface-soft`、`--surface-raised`、`--surface-hover`、`--chrome-bg` 与 `--menu-bg`，不要在组件里继续散落新的纯白或纯黑值。

电子纸模拟使用低饱和设备色板。纸白不是发光纯白，黑色接近炭黑；但设备有效区在四幕之间必须保持同样明亮，不能在第四幕突然变脏或变灰。

### 2.3 黑白主题

- `src/components/ThemeToggle.astro` 是唯一主题开关，使用圆形日月开合图形，不引入图标包或文字胶囊。
- 开关位于产品页语言菜单旁，同时出现在 Docs 与 404 页头；使用原生 `button`、本地化 `aria-label` 和 `aria-pressed`。
- 首次访问跟随 `prefers-color-scheme`，手动切换保存到 `localStorage` 的 `peanup.theme`，刷新与跨页面导航后继续沿用。
- `BaseLayout.astro` 在首屏样式应用前写入 `data-theme`，避免由白到黑的闪烁；`chrome.ts` 负责控件状态、主题色和系统偏好变化。
- 暗色只反相页面纸张、文字、线框和结构性面板。产品摄影、E6 六色色板、电子纸有效区、手机模型和硬件实体样板不得套用全局反相滤镜。
- 主题在开关位置建立圆心。支持 View Transition 时，用 500ms 的圆形页面快照揭幕完成切换：圆内是完整新主题，圆外是完整旧主题，背景、文字、按钮和图片沿同一边界更新，不能把新主题文字提前叠到旧主题背景上。不支持该能力的旧 WebView 使用 320ms 同步颜色渐变，不恢复遮挡正文的独立圆幕节点。明暗主题各有一张 120 × 182 的轻量真实预览，切换开始时立即同步显隐；白天高清首图保持高优先级，暗色高清首图使用低优先级并在空闲时解码，解码成功后再无跳动接管。不得重新把主题切换阻塞在高清图 `decode()` 上。主题按钮是用户主动操作；`prefers-reduced-motion` 下仍保留 340ms 紧凑揭幕，不能退回无反馈的瞬间跳变，其余闲置动效继续遵守系统偏好。
- 明暗首图均使用 `740 × 1120` 画布、相同的 `object-fit: contain` 和定位规则。暗色抠图已经在资源层校准，不再增加单独的 CSS 拉伸或位移补偿。404 页在明暗主题下都使用透明抠图，避免亮色摄影底板形成方形白边。

### 2.4 字体与尺寸

- 展示标题：`Songti SC`、`STSong`、`Yu Mincho`、Georgia 或 serif。
- 正文、导航、按钮：Apple 系统字体栈。
- 技术标签：系统等宽字体。
- 所有文字保持 `letter-spacing: 0`。
- 正文通常为 14–16px；备注、Tab、规格标签不能缩到难以阅读。
- Hero 大字只用于真正的首屏，不进入紧凑卡片、侧栏或工具栏。
- 简体中文规格标题固定为“小到随手带走，/ 也能装下完整的一天。”，使用 Iowan Old Style / 宋体轻字重，两行独立成块并让第二行轻微缩进；不得退回粗黑无衬线或把两行强行拉宽。

通用圆角上限为 6px。分区依靠留白、细线和网格建立层级，不把每段内容包成卡片。阴影只表达实体高度，不制造霓虹悬浮感。

### 机模校准（当前实现）

机模的透明 PNG alpha 边界是几何基准；机框源图保留 `662 × 1380` 的物理画布，容器保持相同源图比例。App 内容层按透明抗锯齿边缘做视觉校准，使用 `left 4.2% / top 2.53623% / width 91.6% / height 94.92754%`（像素框约 `28,35,606,1310`），让玻璃贴合两侧金属边而不显得厚重。机框图片使用 `object-fit: contain`，不做横向拉伸；外框不再叠加自绘黑色底层，机身 R 角完全交给 PNG；屏幕开口使用 `13.8% 13.8% 9.35% 9.35% / 4.85% 4.85% 3.2% 3.2%` 的不对称圆角，分别贴合上弧和下弧。状态栏完整固定在 `402 × 62 pt` 坐标系内，使用本地透明资产；素材失败时才切换为同坐标的 CSS 兜底。

Apple 生态区的手机样板使用 Apple 公布的 `71.9 × 150.0 × 8.75 mm` 机身、`6.3 英寸`显示器与 `1206 × 2622` 像素比例，屏幕逻辑尺寸固定为 `402 × 874 pt`。外层使用本地裁剪后的 `662 × 1380` 轻量机框图来保留钛金属边、R 角与按键细节，容器采用源图 `662 / 1380` 比例；实时 App 层使用经过抗锯齿补偿的透明开口 `约 28 × 35 × 606 × 1310 px`。状态栏完整保持在 `402 × 62 pt` 坐标内，透明资产整条左移约 `12pt`，使 `9:41`、蜂窝、Wi-Fi 与电池都落在两侧约 `24pt` 安全区，失败时才切换 CSS 兜底。桌面机模宽度为 `288–328px`，平板上限 `304px`，移动端最多 `238px`，均受视口高度约束并保持 662:1380 比例。灵动岛覆盖层默认保持紧凑胶囊并合并镜头高光，`04 / TRANSFER` 时在同一坐标横向展开显示 68% 进度，避免源图分离镜头造成错位。机框资源加载期间由极细 CSS 轮廓维持占位，解码完成后自动消隐，不让屏幕内容先出现而外壳空掉。

状态栏使用与 `402 × 874 pt` 显示坐标系一致的本地 `iphone-17-pro-status.png` 透明资产（`1056 × 163 px`），完整保留蜂窝、Wi-Fi、电池和 `9:41` 的原始相对位置；整条资产向左平移约 `12pt`，把两侧 glyph 放回约 `24pt` 安全区，资源失败时才使用同坐标的本地 CSS 兜底。一体式紧凑灵动岛采用 `x 138 / y 14 / width 126 / height 37 pt` 坐标，覆盖层换算为 `top 4.057% / left 35.848% / width 28.455% / height 4.019%`，静态和传输两种状态共用同一基准。App 内容从 `62 pt` 顶部安全区之后开始；显示窗口 R 角使用开口上下不对称曲线，不能误用外层机框宽度。首屏规格带中的屏幕、PPI、厚度、重量与 BLE 仍是可键盘访问的章节入口。桌面章节维持手机和四步轨道并排，`1100px` 以下先显示四步轨道，再显示机模。四个控制项和四个手机界面使用完整 `tab / tabpanel / aria-labelledby` 语义，选中状态、roving tabindex、屏幕内容与 `aria-live="polite"` 状态播报保持同步。机内根视图使用 `overflow: clip`，触摸表面保留 `touch-action: pan-y`；不得安装机模运行时、图标包，或为了“更像手机”牺牲页面滚动。

四幕动画属于用户直接控制的产品演示：滚动决定雨滴和飞燕进度，点击决定写屏与花枝生长，拖动决定琴弦余波。它们不得因为浏览器或 WebView 报告 `prefers-reduced-motion` 而被替换成静态完成帧；该偏好只停掉枝条的闲置摇摆。舞台离开视口或页面隐藏时仍必须暂停 Canvas，避免后台持续耗电。

Docs 导航有两层，但每一层只回答一个问题：固定顶栏负责跨文档移动，正文顶部“本页目录”负责当前文章内的章节定位。`901px` 以上在“产品页”左侧固定显示“使用指南 / 内容玩法 / 六色画面 / 快捷指令”四个高频 Tab；Tab 之间用稳定留白分组，当前页只用 1px Peanup 蓝线标记，不能恢复方格化边框。旁边的文档切换器在主入口显示“全部文档”，进入工程、隐私或排查等次级页面时直接显示当前文档名称并增加同样的蓝色状态线。完整菜单使用三列编辑式索引，以横向细线分隔条目，不把每个入口做成卡片。`701–900px` 保留可见的 `EN / 中` 语言入口，同时改用“指南 / 玩法 / 六色 / 快捷”短标签；`700px` 以下才切换为两层移动顶栏，第一层保留产品页、语言、主题和完整目录，第二层使用四个等宽高频入口，并在最右侧显示“目录”或当前次级文档。`820px` 以下正文侧栏收成单行本页目录；玩法不能重新藏进完整菜单。

移动完整菜单与 `DocsMap.astro` 固定使用同一套 `4 + 4 + 4` 分组：核心指南、使用与连接、规格与公开；菜单第一行直接提供“返回产品页”和语言切换。`820px` 以下正文目录收成单行当前章节，展开后才显示两列章节表，并由 `docsNavigation.ts` 随阅读位置同步编号和标题。产品指南首屏保留四条高频任务；十二个专题在 `821px` 及以上直接展开为编辑式索引，`820px` 及以下才收进“浏览全部文档 / Browse all documents”原生 `<details>`。HTML 默认保留完整内容，`docsNavigation.ts` 只在窄屏收起，因此脚本失败时也不会出现空白目录；打印时始终显示全部专题。跨文档菜单和本页目录必须使用不透底的 `var(--surface)` 纸面，不能让正文透出干扰选项，也不能把跨文档入口与本文章节混在同一个菜单里。

产品页技术章节顶部固定保留 `图像处理 / 六色算法 / 硬件能力 / 目标规格` 四段轻量锚点索引。桌面为单行四列，手机为两行两列；它只承担长章节定位，不显示第三套章节状态，也不引入滚动监听脚本。

Apple 章节标题固定由两个独立的块级短语组成，不能依靠浏览器在句中随机换行。`360px` 下每个短语都必须只占一行；简体中文当前为“拍下此刻，/ 之后自动更新。”，其他五种语言也使用同样简短的两拍节奏。修改文案后必须用文本 Range 的 `getClientRects()` 重新检查六种语言，而不是只看标题外框高度。

手机内部有四个状态：本地整理、内容架、快捷指令、传输进度。内容架把照片、日历、天气和书摘组成 2×2 本地界面，只复用页面已有数据与 CSS，不增加图片请求。桌面端四步轨道位于手机右侧；`1100px` 以下改为手机上方的横向原生 Tab，让控制项与被控制的屏幕在同一阅读顺序里。控件必须同步更新手机界面、设备的 `data-state`、`aria-selected`、焦点顺序和顶部状态；切换使用一次约 520ms 的电子纸写屏扫描，不自动轮播。进入 `04 / TRANSFER` 时灵动岛展开并显示当前进度，切换回其他状态后恢复紧凑形态。触摸设备允许在模型上横向轻扫切换，但必须保留 `touch-action: pan-y`，不能锁住纵向页面滚动。下方微信小程序与更多内容来源只作为跨平台补充，不与四步主流程争夺层级。中英文 `/docs/compatibility/` Apple 小节必须保留与手机一致的四步平面流程，手机端为 `2 × 2`，用于在没有脚本和模型动画时仍把体验路径说清楚。`prefers-reduced-motion` 下取消扫描与位移动画，但保留即时状态切换。

Apple 章节下方还有一条“一天中的四次更新”时间轨道：07:30 天气、12:40 照片、18:30 一日总结、22:30 书摘。它不是自动轮播组件；点击、方向键、Home 与 End 会更新当前时间、手机内 `NEXT FRAME` 标题并播放一次短写屏扫描。时间轨道用于说明自动化的日常价值，不代表四种内容上限，也不能取代快捷指令公开文档。

照片、日历、天气和书摘的模式演示之后必须保留省略号式开放轨道。它只用细线、轻字重和 `ProductCopy.content.more` 的六语短文案说明“四种只是开始”，不能再做成第五张功能卡，也不能暗示尚未公开的内容 API 已经可用。中英文产品 FAQ 使用相同口径：只要最终 App、小程序或快捷指令能够生成一幅 528 × 792 六色画面，就可以成为下一帧；具体内容来源以正式版本为准。

产品页固定顶栏用 2px 六色进度线表达长页阅读位置，并用 `aria-current="location"` 标记当前章节；进度更新必须合并到一个 `requestAnimationFrame`，不能为滚动再引入动画库或高频布局测量。

产品页移动顶栏固定使用 `44 / 76 / 44px` 三列操作栅格。文档入口桌面显示完整本地化名称，手机显示短标签：简体“文档”、繁体“文件”、日文“文書”，英文、德文和法文统一为 `Docs`；链接本身的 `aria-label` 仍使用完整名称。语言名称与 `6px` 箭头轨道必须作为一个整体在 76px 中居中，箭头不能覆盖最后一个字，也不能让 `Dokumentation`、`Documentation` 等长词与当前语言粘连。

六色色板保持平面材料样本，不使用悬浮、缩放、滤镜、阴影或循环动画。桌面色板最终最小高度为 `209px`；手机两列布局中每个样本最小高度为 `160px`。展示高度可以增加，但 Canvas backing store 不跟随设备像素比膨胀。

## 3. 信息架构

首页固定为八个连续故事章节，不再按每个功能拆成独立卡片区：

1. **产品初见**：品牌、核心表达、真实产品、`Coming to Kickstarter` 与 `3.68” E6 / ≈258.7 PPI` 规格带。
2. **电子纸材料**：六种独立颜料纹理、反射式环境光观看与刷新后保持。
3. **四幕体验**：六色云雨、花枝与花瓶、飞燕与琴弦，以及访客自己的照片或文字。
4. **生活场景**：真实摄影把产品放回冰箱、桌面与随身空间。
5. **内容与 Apple 生态**：照片、日历、天气、书摘、iOS 快捷指令、App 内传输进度、微信小程序、第三方扩展与本地隐私依次展开。
6. **本地图像处理**：整理画面、六色成像、BLE 分块传输、刷屏保持；首页只保留 Ordered、误差扩散和感知量化三类策略，完整矩阵深链工程 Docs。三类策略必须可点击和键盘选择，选中后同步重算上方六色成像主预览并更新技术标签；默认 Floyd–Steinberg，不自动轮播。
7. **硬件与规格**：BLE、CUID NFC、Qi、磁吸摆放和完整众筹目标规格合并在同一章，不重复解释。
8. **品牌结尾**：实体设计摄影、Peanup 含义、众筹预览状态、产品指南、众筹状态、联系入口与页脚。首屏的 Kickstarter 状态同样直接进入众筹状态页，并保持至少 44px 的触控高度。工程文档已经在技术章节深链，不在结尾再次抢占普通访客的行动路径。

章节内部仍可包含语义化 `section`，但视觉上必须是一条连续叙事。不得把内容重新拆成十余个互不关联的浮动模块。

规格章的十二项参数之后必须保留三层证据轨道：厂家面板规格、Kickstarter 目标、开放支持前公布。它们分别进入当前语言的产品指南规格锚点、众筹目标锚点和支持前清单锚点。桌面使用开放式三列细线，移动端转为三段纵向轨道；不增加卡片底、状态徽章或大面积强调色。轨道用于让访客理解证据来源，不重复规格表，也不把目标写成已完成事实。

公开产品页面优先写结果和体验，不放协议风险清单。中文公开产品说明位于 `/docs/`；内容玩法位于 `/docs/content/`；六色画面指南位于 `/docs/image-guide/`；硬件与电源位于 `/docs/hardware/`；兼容与平台位于 `/docs/compatibility/`；连接与排查位于 `/docs/troubleshooting/`；术语与规格口径位于 `/docs/glossary/`；众筹状态位于 `/docs/campaign/`；中文工程文档位于 `/docs/engineering/`；中文隐私与数据说明位于 `/docs/privacy/`。英文对应页面位于 `/en/docs/` 下的同名路径。

众筹状态页是普通访客理解项目成熟度的唯一集中入口，不是工程风险清单。它必须分开呈现厂家面板事实、当前产品方向、Kickstarter 目标和支持前必须公开的结果；价格、运费、地区、交付日期、认证和量产状态没有确认前必须明确写作“尚未公布”，不得用占位数字补齐版面。该页使用 `WebPage`，并通过可见 FAQ 同步生成 `FAQPage`。

内容玩法页是首页“照片、日历、天气、书摘”演示的完整客户说明，不是模板商店或功能承诺清单。它必须把六类示例、一天中的内容节奏、E6 构图建议、来源与网络边界、快捷指令路径和“示例不是上限”连成一篇；照片与文字标明本地处理，天气与在线书摘标明需要内容来源，自动化标明受 iOS 权限与触发规则约束。首页内容章节、产品指南、DocsMap 和共享前后篇导航都要能到达该页。

六色画面指南是内容页与工程算法之间的客户层说明。它必须明确 528 × 792 对应 2:3 竖向输出、网页舞台不是实物比例尺、完整显示和铺满画面都保持原始比例、Ordered / 误差扩散 / 感知量化分别解决什么问题，以及发光网页预览不能替代反射式实物色样。该页复用现有照片素材和纯 CSS 六色方法图，不增加运行时脚本；中英文内容页、产品指南、工程抖动章节、DocsMap 与共享前后篇都要能到达它。

硬件与电源页是实体产品的使用说明，不是另一份平台兼容表。它集中解释 3.68 英寸 E6 面板、一次更新从手机计算到断开保持的能量路径、续航变量、Qi 对位与温升边界、磁吸表面差异、MagSafe 只表示机械配件兼容、保养和“厂家面板 / Kickstarter 目标”证据层级。未经验证的输入功率、充电时间、户外能力或整机认证不得提前写成最终事实。

兼容与平台页是众筹访客选择入口的说明，不是重复硬件页。它只集中说明 iPhone / iPad 主要路径、Apple 快捷指令、微信小程序、网页预览边界、BLE 权限与近场更新、第三方接入方向和 CUID NFC 授权条件；Qi、磁吸、续航与保养必须深链硬件页，不在兼容页重新展开。

连接与排查页必须先保留可打印的四步静态清单，再使用共享 `TroubleshootingGuide.astro` 提供六阶段诊断轨道：发现、连接、发送、写屏、Qi 和 NFC。一次选择只更新一个下一步、一个注意项和一个指向完整章节的深链；不得变成问卷、聊天窗口或卡片墙。控件使用原生按钮、roving tabindex、`aria-pressed` 与 `aria-live`，支持点击、方向键、Home 和 End；不保存诊断状态、不联网。桌面为六列开放式轨道，`820px` 以下三列，`560px` 以下两列；每个手机触控区至少 44px。打印时隐藏交互轨道，保留下方完整静态内容。

Apple 快捷指令与自动化说明独立位于 `/docs/shortcuts/` 和 `/en/docs/shortcuts/`。产品指南的 Apple 生态与内容章节必须深链这两页，快捷指令页继续说明一次更新的动作模型、四条日常路径、权限、失败处理和本地隐私边界，不能写成已经在 App Store 发布的最终接口。页面在日常示例和动作草案之间使用共享 `ShortcutPlanner.astro` 提供“触发 / 内容 / 写屏方式”三段流程试搭；三组各四项，实时组合完整流程和成立条件。控件使用原生按钮、`aria-pressed`、roving tabindex、方向键、Home、End 与 `aria-live`，不创建真实 Shortcut、不连接设备、不保存也不联网。桌面保持三列开放式轨道，`820px` 以下改为单列分组和每组 `2 × 2` 选项；打印隐藏选择控件，但保留默认流程摘要。

隐私页是独立可引用的公开页面，不是产品指南里一段重复文案。它必须保持以下边界：

- “当前网站事实”与“App / 硬件产品目标”分开标注。
- 当前网站只保存 `peanup.theme` 和 `peanup.locale`；没有账号、表单或第三方分析 SDK。
- 第四幕照片只在浏览器 Canvas 中处理，不上传，也不写入站点存储。
- 托管 / CDN 可能保留标准访问日志，不能把“无分析 SDK”夸大成“服务器没有任何日志”。
- App 的本地处理、BLE 完成帧、作品记录、系统权限和内容服务都是发布前需按真实实现复核的目标。
- CUID NFC 的授权、标识与通行日志由门禁管理方控制，Peanup 不能授予或绕过门禁权限。
- 隐私页使用 `WebPage`，产品与工程文档继续使用 `TechArticle`，不能混用结构化数据类型。

## 4. I18N 与路由

产品页使用 `src/i18n/product.ts` 中的类型化构建期字典，核心类型为 `Locale`、`ProductCopy`、`ProductSpec` 和 `CanvasSceneCopy`。不要引入 i18next、React 或运行时翻译库。

| 语言 | 路由 |
| --- | --- |
| 简体中文 | `/` |
| English | `/en/` |
| 繁體中文 | `/zh-tw/` |
| 日本語 | `/ja/` |
| Deutsch | `/de/` |
| Français | `/fr/` |

`/zh-cn/` 是旧简体中文链接的兼容别名：页面继续可访问，但 canonical 固定指向 `/`，robots 使用 `noindex,follow`。它不进入 sitemap，也不能被统计为第七个正式产品路由。

根路径的静态内容与 `x-default` 为简体中文。首次进入根路径时会先读取用户保存的语言，其次根据可识别的 IANA 时区与 UTC 偏移判断地区，最后参考浏览器语言；无法判断时保持简体中文。手动语言选择写入 `peanup.locale`。语言菜单只使用文字名称，不用国旗。新增文案时必须检查德语长词、法语换行、日语断行和中英文标点。

## 5. 屏内四幕

四幕是 Peanup 电子纸内容，不是网页背景。原生 Canvas 2D 必须严格限制在产品显示区域内，禁止扩展为首屏或全页装饰动画。场景内部以厂家面板的 528 × 792 像素矩阵组织细节；网页舞台继续使用更适合观看的 3:4 编辑式展示比例，不把 3.68 英寸物理尺寸换算成 CSS 尺寸。

### 5.1 统一写屏过渡

`01 ↔ 02 ↔ 03 ↔ 04` 的任意双向切换都使用同一套 880ms 写屏进度和扫描线：

1. `01–03` 离场时缓存旧 Canvas 帧；`04` 离场时保留完整在线工作区 DOM。
2. 准备目标帧。
3. 以自下向上的写屏遮罩和扫描线替换旧帧。
4. 完成后保持静态画面。
5. 进入 04 时，在线工具栏在写屏结束后再淡入。

进入 04 时在线工作区按同一进度由下向上展开；离开 04 时则按该进度向上收起，因此视觉节奏与 Canvas 快照一致，但不会把可交互表单错误地复制进 Canvas。滚动与点击共享同一场景状态；点击 Tab 时先锁定目标幕并立即开始写屏，再平滑移动页面，不能在途中重放经过的其他幕。左侧四项使用原生 `tablist / tab / tabpanel` 语义与 roving tabindex，支持点击、触摸、Enter、Space、方向键、Home 和 End；选中项、面板标签与焦点必须同步。不允许 03 → 04 使用另一套时序，也不允许切换后跳回第一幕。

画面左下使用“功能 / 场景”结构，例如“自由创作 / 飞行”；右下单独显示 `01 / 04` 至 `04 / 04`，不要在左右两处重复页码。舞台画框沿用线上认可的视觉尺寸：桌面约 `510 × 680px`、移动端约 `315 × 420px`。它是用于看清内容和完成交互的演示舞台，不按真实 3.68 英寸尺寸或面板外形比例硬缩。

移动端四个场景 Tab 使用同一条 `68px` 轨道：编号占一行，标题可占一至两行，说明文字在该断点隐藏。英文、德文与法文的两行标题必须完整位于蓝色选中线之上；不能为了压缩舞台上方高度退回 `60px` 固定行或依赖浏览器默认行高。

### 5.2 01 六色云雨

- 初始以六色云彩为主，云层不能遮满屏幕。
- 继续滚动后，48 个大小、长度与节奏不同的六色雨滴分段下落。
- 雨滴是独立细长形状，没有连接线，不形成永久悬挂的帘子。
- 雨量是淅淅沥沥，不是暴雨。
- 横向风偏移只由滚动触发；停止滚动后雨滴静止，不做无限循环。

### 5.3 02 日常信息与花枝

- 5 条花枝从花瓶附近向光生长，花瓶必须保留。
- 枝条在小屏幕里需要清晰，但不能过长、过粗或越过有效区。
- 左上显示日期与天气，下一层承载书摘。
- 场景完成短时生长后保持，不做无意义循环摇摆。

### 5.4 03 飞燕与琴弦

- 30 只黑色飞燕分批沿弧线进入，不能遮黑整块屏幕。
- 每 3 只中有 1 只在飞行后停到谱线附近，即最终保留 10 只停鸟；其余飞出画面。
- 保留六色场景关系，不能退化为单一黄色画面。
- 点击、触摸或按住拖动会在指针附近触发约 900ms 的拨弦余波；动效结束后回到静态。

### 5.5 04 在线体验

- 在线体验直接位于同一块产品屏幕，不另建工作台页面。
- 照片选择区与文字输入区始终并排显示，不要求访客先发现模式开关。
- 支持选择或拖入 JPG、PNG、WebP、AVIF，以及输入短文字。
- 图像只在浏览器本地裁切、六色量化与抖动，不上传。
- 保持原始比例并覆盖式裁切，禁止压扁画面。
- 工具栏、提示和 Tab 的字号、边距与前三幕一致。
- 120 字以内的文本需要在 528 × 792 画布内自动换行与缩放；超出可读安全区时使用省略号提示，不能静默丢失末尾内容。

### 5.6 Canvas 运行边界

- 四幕初始 HTML 必须带有零额外请求的静态首帧。首帧使用 CSS 与现有六色色值绘制云层和电子纸纹理；异步 Canvas 就绪后仅淡入接管，不能改变设备画框尺寸或让慢速 WebView 先出现空白。
- 当前三幕自然场景保留 `heroSketch.ts` 原有的 p5 实例模式绘图语义，但由项目内 `miniP5.ts` 提供所需的最小 Canvas API，不再加载完整 p5 包或 `@types/p5`。动画在四幕距离视口约 `1000px` 时延迟加载；HTML 静态首帧负责更早的弱网占位，不能为了预热把三幕脚本重新拉回首屏。12 种雨滴轮廓合并在本地 `/assets/rain-drops.webp` 图集里，只产生一次素材请求，没有第三方运行时请求。
- `miniP5.ts` 只覆盖四幕实际使用的生命周期、路径、文字、图片染色、高 DPI Canvas 和确定性随机数。扩展动画时应按需补充这一兼容层，禁止直接恢复完整 p5 依赖。
- 逻辑画布固定为 `528 × 792`，设备像素比最多取 `2`；桌面绘制上限为 42 fps，窄屏为 30 fps。
- 纸纹与六色层使用缓存；仅在滚动、切幕、拨弦和短时入场期间请求动画帧。
- Canvas 离开视口或页面切到后台时暂停。
- 四幕属于用户直接操控的产品体验；即使浏览器报告 `prefers-reduced-motion`，写屏、花枝生长、飞燕飞行与拨弦响应仍由滚动、点击和拖动驱动。该偏好只移除花枝的闲置摇摆；页面离开可见区或切到后台仍立即暂停绘制。
- 素材由代码绘制，不把第三方网络可用性变成页面依赖。

## 6. 电子纸处理与技术展示

电子纸不是普通照片加噪点。当前表现分为：

1. 将输入映射到校准后的六色颜料色板。
2. 使用 Ordered、Floyd–Steinberg、Stucki 或感知量化等方式重建明暗和局部颜色。
3. 在设备外壳内加入克制纸纹、写屏扫描线和实体阴影。

Canvas 使用 528 × 792 内部逻辑画幅并按设备像素比绘制。该数值对应厂家面板像素矩阵，但不能单独用于推导真实 BLE 负载或传输时间。外层 CSS 使用 3:4 产品舞台保证网页构图与可读性，这只是演示比例，不是新的硬件规格；照片仍使用明确的 `cover` 或 `contain`，禁止无意压扁内容。

“计算留在手机上”四步必须保持因果关系：

- `01 整理画面`：动漫头像或壁纸输入。
- `02 六色成像`：同一构图经过算法转换，不能改变高度或过曝。
- `03 分块传输`：已打包像素通过 BLE 发送。
- `04 刷屏并保持`：真实生活照片置于带 PEANUP 标签、内边距和阴影的设备边框。

## 7. 硬件能力卡

“画面一直在，连接只在更新时”下方四项使用统一灰阶硬件组件：

- 桌面维持四列立方体，手机改为 `2 × 2` 紧凑材料板；`390px` 高度约 `530px`，`360px` 高度约 `562px`，不能退回四张连续全宽卡。
- 手机卡面仍可点击、触摸和键盘翻转。六种语言的背面正文必须完全位于卡面内，不能通过隐藏溢出掩盖裁字。
- `#hardware` 与 `#specifications` 是公开稳定锚点，固定页头下需保留滚动留白；Docs 可以深链这两个位置。

- 正面纯白，1px 灰线和轻阴影。
- 背面固定使用 `#ecece8`，不使用黄、红、绿、蓝顶边或撞色反转。
- 图标为石墨灰 56–64px，只使用四个本地 SVG：Bluetooth、NFC、BatteryCharging、Magnet。
- 悬浮、点击、触摸、Enter 和 Space 均可翻面，`aria-pressed` 同步状态。
- 焦点只使用低饱和 Peanup 蓝。
- `prefers-reduced-motion` 下用正反面淡入切换，不做 3D 旋转。

不要为四个图标重新安装完整图标包。

## 8. 响应式规则

必须检查以下基准视口：

- `1920 × 800`，用于校验超宽但不高的首屏构图
- `1440 × 900`
- `1366 × 1024`
- `1024 × 1366`，同时代表 iPad 竖向布局
- `390 × 844`
- `360 × 800`

`1592px` 以上的首屏、固定页头、底部规格带和两侧装订线使用独立的故事画框：左右至少保留 `76px`，画框最多放宽到 `1920px`。正文与四幕等阅读内容继续使用 `1440px` 上限，避免带鱼屏首屏出现大片无意义空白，也避免正文行长失控。

移动端不是桌面的等比缩小：

- Hero 使用稳定文档流与网格，标题、产品图、状态和规格带不能互相挤压。
- 四幕先给出标题与四个 Tab，再显示完整设备画板。
- 技术流程、抖动、硬件能力、规格和 Docs 转为单列或受控双列。
- 中文展示标题使用短语感知与均衡断行，不能拆开“亮着”等完整词组；`360px` 下不得把“纸。”单独留成一行。
- Apple 章节的两段标题短语在六种语言中都必须各占一行；不能拆开“自动更新”、德语动词或法语省音词。
- Docs 的兼容矩阵和多列表格可以横向滚动；两列产品规格表必须直接适配可用宽度，不能让规格值藏在横向滚动区。
- 中英文产品指南首屏使用两段块级短句；`360px` 下中文为两行、英文也不得把单个短句拆开。不要用缩小全部 Docs 正文的方式处理标题。
- 正文保持可读字号，触摸目标足够大；长词换行，不允许按钮文本溢出。
- 固定格式组件设置 `aspect-ratio`、网格轨道或 min/max 约束，防止动态内容引发布局跳动。
- 页面不得出现水平溢出、文字遮挡、Canvas 空白或图片异常裁切。
- 404 也属于品牌体验：桌面维持左右编辑式构图，`360px` 以下标题保持两段完整短句，操作按钮不得制造横向溢出；明暗主题都只使用透明抠图。

## 9. 图片、加载与保护边界

网页资产位于 `public/assets/`。产品、手机和手持图的固有尺寸分别为：

- 产品：`740 × 1120`
- 手机：`1122 × 1402`
- 手持：`1122 × 1402`

资产规则：

- 产品必须在第一屏明确出现。
- AVIF 优先、WebP 回退；白天高清首图高优先级，暗色高清首图低优先级，其余大图使用 `loading="lazy"` 与 `decoding="async"`。
- 明暗首图各带 120 × 182 的 WebP 真实预览；手机与手持摄影各带 180 × 225 的 WebP 真实预览。结尾两张预览合计不足 5 KB，以 `loading="eager"`、`fetchpriority="low"` 和同步解码提前形成弱网首帧；高清图仍使用延迟 `data-src/data-srcset`。正式图片只有在加载成功后才显现，失败时继续保留预览，禁止出现空白框或破图图标。
- 图片不沿用 Potato Card、TodooCam 或其他旧品牌文字。
- 不使用 PDF 整页截图，不存摄影原片，只保留网页需要的分辨率。
- 所有展示图片补齐真实 `width`、`height`、alt 和合适裁切。
- OG 使用单独的低分辨率品牌分享图，不直接暴露高质量产品摄影。

所有 `img` 使用 `draggable="false"`；`img`、`picture` 和 `.protected-media` 使用 `user-select:none`、`-webkit-user-drag:none` 与 `-webkit-touch-callout:none`。所有页面共享的 `chrome.ts` 再次把图片设为不可拖拽，并只在事件目标属于 `img`、`picture` 或 `.protected-media` 时阻止 `dragstart` 与 `contextmenu`；它不是全站禁右键。页面使用 `noimageindex`、`max-image-preview:none`。`/assets/*` 的 `X-Robots-Tag` 由 `vercel.json` 或生产 Nginx 提供，Astro 静态文件本身不会自动带上该响应头。

这是体验和索引控制，不是 DRM；`noimageindex` 与 `X-Robots-Tag` 也只对遵守规则的爬虫有效。公开网页无法阻止 Network、缓存、源码或截图取得已经传输的图片。禁止使用 Base64、透明遮罩或全站禁右键冒充真正的下载保护；正文选择、链接、键盘导航和屏幕阅读器不能因此受损。

## 10. 性能架构

Astro 负责静态 HTML、SEO 与多语言路由。屏内三幕使用本地 `miniP5.ts` 驱动原生 Canvas 2D，不携带完整 p5 运行时或类型包；不引入运行时翻译框架、图标包或静态 Vercel adapter。

样式分层：

- `src/styles/base.css`：Token、全局壳层、导航、页脚、媒体保护和返回顶部。
- `src/styles/global.css`：产品首页布局、场景和响应式状态。
- `src/styles/docs.css`：二十六条 Docs 路由共享的壳层、文章与响应式样式。
- `src/styles/docs-content.css`：内容玩法页的六种内容图样、时间轨道与专属响应式规则，只由中英文内容页加载。
- `src/styles/docs-engineering.css`：算法矩阵、BLE 状态机、功耗测量与验证边界等工程页专属样式，只由中英文工程文档加载。

`src/scripts/lazyFeatures.ts` 负责接近视口时加载：

- `epaperShowcase.ts`：六个静态电子纸颜料样本，只在色板接近视口时加载。样本不响应悬浮、不缩放、不加滤镜或阴影；当前桌面 backing store 合计约 `0.09 MiB`，手机约 `0.06 MiB`，无需六张额外位图或第三方请求。
- `ditherShowcase.ts`：三种抖动样例与选择状态，只在算法展示接近视口时加载；选择后通过轻量自定义事件通知主转换流程，避免访问上半页时执行图像量化。
- `heroSketch.ts`、`miniP5.ts` 与 `interactiveScreen.ts`：延迟加载的三幕绘图、轻量 Canvas 兼容层与在线预览。
- `phoneShowcase.ts`：Apple 生态四状态切换、四时刻自动化轨道、键盘焦点与短写屏扫描，接近章节时加载。
- `epaperPipeline.ts`：图像转换流程、三种算法重算与短写屏反馈。
- `hardwareCubes.ts`：硬件翻转交互，支持点击、Enter、Space 与 `Esc` 收起。

常规动态模式下，硬件翻转交互在组件接近视口时加载；`prefers-reduced-motion` 开启时则立即加载，让无 3D 的淡入切换在首次键盘、触摸或鼠标操作前就已可用。

性能约束：

- 线上首屏传输目标仍为 `<70 KiB Brotli`，并由 `scripts/verify-site.mjs` 作为构建门槛执行；不能只把它写在文档里。移动端必须用禁用缓存的新页面验证真实请求：首屏不得出现 `heroSketch`、`interactiveScreen`、`miniP5`、`hardwareCubes` 或雨滴图集；滚动后允许在四幕距离视口约 `1000px` 时预热。暗色高清首图不在关键首屏内，在空闲或切换主题时解码。后续不得再把明暗两张高清图同时计入默认首屏，也不能用隐藏资源的总大小冒充真实加载瀑布。
- 首屏与常驻交互 JavaScript 目标 `<50 KB gzip`；三幕场景必须保持独立异步 chunk，不能进入首屏关键路径。
- 2026-08-01 当前构建基线：常驻产品脚本合计 `4,395 B gzip`，约 `4.29 KiB`；三幕主动画 chunk 为 `28,206 B` 原始、`10,505 B gzip`；包含主动画、在线体验、手机四状态和可切换算法在内的七个按需交互 chunk 合计 `56,623 B` 原始、`22,933 B gzip`。Astro 会把 Docs 小型页面交互内联进 HTML；校验器必须把最重路由的内联模块加入总量，而不能只统计 `_astro/*.js`。当前最大内联值为快捷指令页 `640 B gzip`，全部交互 JavaScript 的保守口径为 `27,968 B gzip`。产品专用 CSS 为 `20,306 B gzip`，共享基础 CSS 为 `3,135 B gzip`。雨滴图集为 `4,930 B`，只在四幕接近视口后请求。首页 HTML、两份 CSS 与常驻 JS 合计 `42,095 B gzip`，关键首屏 Brotli 估算为 `71,237 B`。公共 Docs CSS 为 `11,930 B gzip`，内容玩法专属 CSS 为 `1,665 B gzip`；众筹页与工程页继续使用各自的页面专属样式，不能把只服务两条路由的规则塞回公共 Docs。构建预算固定为：共享 CSS `<4 KiB gzip`、产品 CSS `<20 KiB gzip`、Docs CSS `<12 KiB gzip`、常驻产品 JS `<6 KiB gzip`、四幕动画 `<11 KiB gzip`、全部交互 JS `<30 KiB gzip`、关键首屏 `<70 KiB Brotli`。校验器必须同步输出 CSS、按需脚本、首屏文本和图片明细，使文档数字能直接从生产构建复核。结尾两张高清摄影使用 `data-src/data-srcset`，只有章节进入预热距离或成为初始深链目标后才请求，轻量预览以低优先级提前取得，负责弱网与快速滚动首帧。这组数据包含当前 528 × 792 材料处理、四幕完整 Tab 键盘语义、10 只停鸟、可见区暂停、静态首帧、统一 04 过渡、手机四状态、三种算法实算切换、章节进度、摄影预热、双语诊断、双语快捷指令流程试搭、双语支持前确认和二十六条 Docs 路由，后续只能在不减少视觉效果的前提下控制增长。

- `pnpm build` 必须在 Astro 检查和静态生成后继续运行 `scripts/verify-site.mjs`。该零依赖校验器覆盖 34 个 HTML 路由、内部链接与锚点、H1、canonical、hreflang、JSON-LD 主类型、FAQPage 非空内容、TechArticle 日期合法性与先后顺序、sitemap、公开页面标题与摘要去重、英文标题和摘要长度、六语产品页众筹入口、四幕 Tab 与六色静态首帧、第四幕输入约束与隐私状态、全部图片防拖拽、零外部运行时素材、404 透明抠图、双语诊断的六阶段顺序与 `aria-live`、双语快捷指令页的 3 组 / 12 项 / 分组语义 / `aria-live` / 动作深链、最重页面内联模块，以及错误复制进生产包的 `.DS_Store`；新增页面或稳定锚点时要同步更新校验清单。
- 零第三方运行时请求。
- 非首屏大图与交互按需加载，首屏产品图不懒加载。生活摄影、技术流程和设计摄影在进入前约 3.5 个视口开始预热；直接访问带章节 hash 的 URL 时，目标章节立即预热，不等待 IntersectionObserver 的下一轮回调。首图与结尾摄影都有轻量真实预览，正式图片以 `data-progressive-image` 标记并在加载成功后显现，避免快速滚动、深链跳转或资源失败时出现白框和破图图标。
- Apple 生态、技术、规格、设计与品牌结尾使用 `content-visibility: auto`，但不能共用一个虚构高度。`global.css` 按实测分别设置桌面、`980px` 以下和平板/手机的 `contain-intrinsic-size: auto <size>`；其中技术章必须包含嵌套规格章的真实高度。调整内容或六语言文案后，应在 `1440 × 900`、`768 × 1024` 与 `390 × 844` 重新测量，避免弱网快速滚动时页面总高度突然扩张。
- 不为简单动效新增框架，不盲删脚本使用的动态状态类。
- `/assets/*` 使用短缓存与 `stale-while-revalidate`；带哈希的 `/_astro/*` 才长期 immutable。

## 11. Docs 与 SEO

- `/docs/` 是默认中文产品说明，使用 `TechArticle`。
- `/docs/hardware/` 是中文硬件与电源指南，使用 `TechArticle`。
- `/docs/compatibility/` 是中文平台与兼容边界，使用 `WebPage`。
- `/docs/content/` 是中文内容玩法与来源说明，使用 `WebPage`。
- `/docs/image-guide/` 是中文六色画面、选图、裁切与预览指南，使用 `WebPage`。
- `/docs/troubleshooting/` 是中文连接与排查指南，使用 `TechArticle`。
- `/docs/support/` 是中文日常保养、支持信息与众筹服务边界，使用 `TechArticle`。
- `/docs/shortcuts/` 是中文快捷指令与自动化指南。
- `/docs/engineering/` 是默认中文工程文档。
- `/docs/privacy/` 是中文隐私与数据说明，使用 `WebPage`。
- `/docs/campaign/` 是中文众筹状态页，使用 `WebPage`；它集中区分厂家事实、众筹目标和支持前公开项，并带可见 FAQ 与六项支持前快速确认。
- `/docs/glossary/` 是中文术语与规格口径速查，使用 `WebPage`；它必须明确 PPI/DPI、网页预览比例、MagSafe 配件兼容和三层规格证据，不能扩写 E6 的未经确认含义。
- `/en/docs/`、`/en/docs/content/`、`/en/docs/image-guide/`、`/en/docs/hardware/`、`/en/docs/compatibility/`、`/en/docs/troubleshooting/`、`/en/docs/support/`、`/en/docs/shortcuts/`、`/en/docs/privacy/`、`/en/docs/glossary/`、`/en/docs/campaign/` 与 `/en/docs/engineering/` 提供对应英文页面。
- Product JSON-LD 只出现在六个产品页。
- 每页提供绝对 canonical、正确 locale、hreflang 和 `x-default`。
- 产品页的 `x-default` 为 `/`；产品 Docs 的 `x-default` 为 `/docs/`；工程 Docs 的 `x-default` 为 `/docs/engineering/`。
- `sitemap.xml` 应包含六个可索引产品路由和二十六条 Docs 路由，`robots.txt` 指向 sitemap。

Docs 与产品页共享纸白/近黑编辑式主题和移动端可访问目录，不把公开产品文档重新变成逆向工程报告。二十六条 Docs 路由统一通过 `src/components/docs/DocsShell.astro` 和类型化 `DocPageConfig` / `DocTocItem` 生成；固定顶栏和移动菜单都必须保留明确的“产品页 / Product”入口，中文返回 `/`，英文返回 `/en/`。宽桌面把使用指南、内容玩法、六色画面和快捷指令作为直达 Tab 放在产品页左侧；其他专题进入“核心指南 / 使用与连接 / 规格与公开”三列完整菜单，深层页让文档切换器直接显示当前文档名称和蓝色状态线。`701–900px` 保留同一行短标签，`700px` 以下才切换为两层移动顶栏：第一层保留产品页、主题和完整目录，第二层由四个等宽高频 Tab 与固定可见的“目录 / 当前文档”组成。产品指南首屏后先给出第一次更新、六色选图、连接排查和快捷指令四条任务入口；十二个专题在 `821px` 及以上直接展开为三列纵向编辑式索引，在 `820px` 及以下才收进原生折叠区。众筹页的 `CampaignDecisionGuide.astro` 一次只展开硬件事实、目标规格、平台、隐私、价格交付或支持服务中的一项，桌面使用问题轨道与结果区，手机保持稳定两列选择。支持页负责日常保养、问题报告和开放支持前的服务边界；其他专题继续各自承担内容、图像、硬件、平台、隐私、术语、众筹与工程边界，不能互相替代。桌面侧栏目录跟随阅读位置，宽度限制在 `220–320px`；`820px` 以下只显示当前章节，展开后才显示两列章节表。顶栏下方使用单色 Peanup 蓝阅读进度线。每篇页底由共享壳层生成前后篇、返回产品页，以及产品页、`i@caiths.com`、GitHub 和对应语言的统一页脚。

兼容与平台页的阅读与无障碍章节必须区分网站能力和实体产品能力：网站支持键盘、触摸、减少动态，并用名称、编号和形状补充颜色；实体电子纸依赖环境光。VoiceOver、Dynamic Type、焦点顺序与对比度属于 App 发布检查，不得写成已完成认证。众筹状态页保留一键复制规格摘要，复制内容必须分开厂家面板事实、众筹目标与条件性能力。每条 Docs 路由输出 `BreadcrumbList`；产品指南固定两级，专题页固定三级，并由 `scripts/verify-site.mjs` 检查位置、名称和绝对 URL。

在 `320px` 视口，中文产品指南 H1 固定保持两行“一张会更新的纸， / 一幅会留下的画。”的阅读节奏，不能压成四行；快速任务区标题启用平衡换行，不能留下孤立单字。这个窄屏规则只修正排版，不缩小触控目标，也不改成横向滚动标题。

手机端需要横向滚动的 Docs 表格必须固定第一列，确保平台、状态或字段名称不会在阅读数值时消失；无需滚动的规格表不启用固定列。桌面文档切换器和手机“目录”都必须保留本地化“打印 / 保存 PDF”按钮，分别提供至少 44px 和 48px 的触控高度；点击后先关闭当前菜单，再调用一次系统打印。打印与“保存为 PDF”使用 `docs.css` 内的纯 CSS A4 版式：隐藏固定顶栏、目录、返回顶部和篇章导航，强制回到白纸、展开 FAQ 答案、移除表格最小宽度，并尽量避免卡片、表格行和说明块跨页断裂。交互复用 `docsNavigation.ts`，不能引入单独脚本、PDF 依赖或服务端渲染。

## 12. 文件责任范围

| 文件 | 职责 |
| --- | --- |
| `src/components/product/ProductPage.astro` | 六语言共享的产品页结构 |
| `src/components/docs/DocsShell.astro` | 二十六条 Docs 路由共享的顶栏、产品入口、语言、主题、阅读进度、目录与前后篇壳层 |
| `src/components/docs/DocsMap.astro` | 中英文产品指南首屏后的四条任务入口，以及“核心指南 / 使用与连接 / 规格与公开”三组十二个专题入口 |
| `src/components/docs/CampaignDecisionGuide.astro` | 双语众筹页的六项支持前确认、状态证据与下一篇深链 |
| `src/components/docs/TroubleshootingGuide.astro` | 中英文连接排查页共享的六阶段诊断轨道与可访问结果区 |
| `src/components/docs/ShortcutPlanner.astro` | 中英文快捷指令页共享的三段流程试搭、结果摘要与打印降级 |
| `src/types/docs.ts` | 类型化 `DocPageConfig` 与 `DocTocItem` |
| `src/i18n/product.ts` | 类型化语言元数据与全部产品文案 |
| `src/pages/index.astro` 与语言目录 | 各静态路由入口 |
| `src/pages/docs.astro` | 默认中文公开产品文档 |
| `src/pages/docs/content/index.astro` | 中文内容玩法、构图、来源与自动化边界 |
| `src/pages/docs/image-guide/index.astro` | 中文六色画面、选图、裁切与预览指南 |
| `src/pages/docs/hardware/index.astro` | 中文 E6 面板、电源、Qi、磁吸与保养指南 |
| `src/pages/docs/compatibility/index.astro` | 中文平台与兼容边界 |
| `src/pages/docs/troubleshooting/index.astro` | 中文首次连接、传输、刷屏、Qi 与 NFC 排查指南 |
| `src/pages/docs/support/index.astro` | 中文日常保养、支持信息与服务规则边界 |
| `src/pages/docs/shortcuts/index.astro` | 中文快捷指令与自动化指南 |
| `src/pages/docs/privacy/index.astro` | 中文隐私与数据说明 |
| `src/pages/docs/glossary/index.astro` | 中文术语与规格口径速查 |
| `src/pages/docs/engineering/index.astro` | 默认中文工程文档 |
| `src/pages/en/docs/index.astro` | 英文公开产品文档 |
| `src/pages/en/docs/content/index.astro` | 英文内容玩法与来源说明 |
| `src/pages/en/docs/image-guide/index.astro` | 英文六色画面、选图、裁切与预览指南 |
| `src/pages/en/docs/hardware/index.astro` | 英文硬件与电源指南 |
| `src/pages/en/docs/compatibility/index.astro` | 英文平台与兼容边界 |
| `src/pages/en/docs/troubleshooting/index.astro` | 英文连接与排查指南 |
| `src/pages/en/docs/support/index.astro` | 英文日常保养、支持信息与服务规则边界 |
| `src/pages/en/docs/shortcuts/index.astro` | 英文快捷指令与自动化指南 |
| `src/pages/en/docs/privacy/index.astro` | 英文隐私与数据说明 |
| `src/pages/en/docs/glossary/index.astro` | 英文术语与规格口径速查 |
| `src/pages/en/docs/engineering/index.astro` | 英文工程文档 |
| `src/layouts/BaseLayout.astro` | canonical、hreflang、JSON-LD、OG 与全局壳层 |
| `src/components/ThemeToggle.astro` | 六语言共享的黑白主题开关与无障碍标签 |
| `src/styles/base.css` | 全站基础样式 |
| `src/styles/global.css` | 产品页设计与响应式样式 |
| `src/styles/docs.css` | 二十六条 Docs 路由共享样式 |
| `src/styles/docs-campaign.css` | 只由中英文众筹页加载的支持前确认样式与打印降级 |
| `src/styles/docs-engineering.css` | 中英文工程文档专属样式 |
| `src/scripts/heroSketch.ts` | 云雨、花枝、飞燕和统一写屏过渡的视觉与时序参数 |
| `src/scripts/miniP5.ts` | 四幕专用的轻量 p5 兼容 API、Canvas 生命周期和高 DPI 绘制 |
| `src/scripts/interactiveScreen.ts` | 第四幕照片与文字本地预览 |
| `src/scripts/epaperShowcase.ts` | 六色色板和材料样例 |
| `src/scripts/ditherShowcase.ts` | 三种按需加载的抖动算法样例、选择状态与主预览事件 |
| `src/scripts/epaperPipeline.ts` | 技术流程六色转换、三种算法重算与写屏状态 |
| `src/scripts/hardwareCubes.ts` | 四个硬件组件的翻转与键盘交互 |
| `src/scripts/phoneShowcase.ts` | Apple 生态四状态、四时刻自动化轨道、焦点、触摸轻扫与写屏切换 |
| `src/scripts/shortcutPlanner.ts` | 双语快捷指令流程试搭的选择状态、结果组合与键盘导航 |
| `src/scripts/site.ts` | 内容模式、慢速滚动与媒体交互保护 |
| `src/scripts/chrome.ts` | 主题状态、页头状态、章节进度与返回顶部 |
| `src/scripts/docsNavigation.ts` | Docs 阅读位置、菜单、打印、复制、诊断轨道与支持前确认交互 |
| `src/scripts/lazyFeatures.ts` | 非首屏功能按需加载 |
| `src/components/HardwareIcon.astro` | 四个本地硬件 SVG 图标 |
| `.github/workflows/deploy.yml` | 构建、不可变 release、原子激活、公网验收与条件回滚 |
| `docs/DEPLOYMENT.md` | 宝塔 / Nginx、TLS、人工验收和回滚手册 |

## 13. 禁止回退的决策

- 不把 Canvas 动画移到产品屏幕外，也不让三幕运行时进入首屏同步加载。
- 不恢复完整 `p5` 或 `@types/p5` 依赖；新增绘图能力时只在 `miniP5.ts` 中补充实际需要的 API，并先做四幕视觉回归。
- 不把三个演示场景描述成产品能力上限。
- 不让雨滴持续循环，不加雨滴连接线，不用巨型云层遮满屏幕。
- 不删除花瓶，不让花枝越过设备边界。
- 不把飞燕画面改成单一黄色或删除黑色主体。
- 不单独新增工作台替代第四幕屏内体验。
- 不让 03 → 04 使用独立过渡；所有场景必须共享写屏逻辑。
- 不把暗色做成孤立黑色分区，不在灰阶硬件卡中突然加入彩色撞色。
- 不把目标规格、平台兼容或 CUID 门禁写成无条件保证。
- 不加入原始摄影文件、外部雨滴请求或完整图标包。
- 不用图片保护措施损害正文选择、键盘或辅助技术。
- 不重置用户工作区，不提交或推送尚未审阅的修改。

## 14. 验收清单

每轮修改后至少运行：

```bash
pnpm check
pnpm build
git diff --check
```

浏览器验收：

- 六个产品语言路由和二十六条 Docs 路由均可直接打开。
- Hero 在六个基准视口无重叠、异常裁切或 CLS。
- 四个 Tab 可滚动、点击、触摸和键盘双向切换。
- 01 ↔ 02 ↔ 03 ↔ 04 均使用自下向上的统一刷新。
- 停止滚动后雨滴静止；离开四幕和切后台后 Canvas 不持续绘制。
- 04 可处理照片和文字，源照片不会离开浏览器。
- 04 离场后工具栏应随写屏完全收起；返回时只在写屏完成后出现。
- 六色转换不拉伸、不过曝且保留主要细节。
- 硬件卡可悬浮、点击、触摸和键盘翻转；减少动态时使用淡入。
- 图片不可选中、拖拽或触发受保护区域右键图片菜单，正文交互不受影响。
- 控制台无相关错误，页面无水平溢出；手机模型上的横向轻扫不能阻断纵向页面滚动。
- canonical、hreflang、x-default、JSON-LD、robots、sitemap 与缓存头正确。
- 三幕运行时保持为四幕附近才加载的独立 chunk；构建结果不包含完整 p5、外部运行时请求或未使用的图标包。
- 主题按钮在产品页、Docs 和 404 均可点击，手动主题刷新后保留；产品摄影与电子纸有效区不被滤镜反相。

## 15. 当前审阅边界

本轮整体打磨完成后先交给用户审阅。未经明确确认，不执行 Git commit 或 push。后续代理必须保留当前未提交内容，并在修改前重新读取工作区状态。

## 16. 客户文案规则

- 一句话只讲一个重点。先说客户能得到什么，再说明技术如何实现。
- 首页不堆叠“链路、路径、能力、接入、验证”等内部工程词汇。算法名称和精确规格可以保留，但解释必须使用日常语言。
- 限制条件独立成句。`3 mm`、`28 g` 和一个月以上续航始终标为 Kickstarter 目标，续航同时注明取决于刷新频率。
- CUID NFC 必须同时说明系统支持、管理方授权和当地规则；MagSafe 只描述配件兼容，不暗示 Apple 认证。
- 英文和其他语言应按当地读者的自然表达重写，不逐字翻译中文。德语等长文本必须在移动端单独检查换行。
- `/docs/` 与 `/en/docs/` 面向客户，保持短句和任务导向；`/docs/engineering/` 保留工程精度，不做营销化改写。

## 17. 一句话设计判断

> 它是否让 Peanup 更像一张安静、真实、会更新的六色相纸，而不是让网页本身抢走产品的注意力？
